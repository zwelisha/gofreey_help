user_input = input("Enter your string input")

def assertive_kata(s1,s2):
    '''
    s1 and s2 here are two input strings passed by the user.
    '''
    # For the first assertion, let's check if the number of chracters is the same.
    #That is the strings, s1 and s2 have the same length.

    assert len(s1) == len(s2)
    
    

